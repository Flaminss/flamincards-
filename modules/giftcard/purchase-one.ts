// export const canUser = (userId: UserId) => {
//
// };

export type PurchaseForm = {
  card: {
    $id: string;
  };
  // cardinfo
  // billing info
};

export async function action() {}

/*
fill form -> set billing info -> confir purchase -> make transfer 
OR
fill-form -> confirm purchase -> deposit to wallet
*/
